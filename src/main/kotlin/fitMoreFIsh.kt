fun main(args: Array<String>) {
    fitMoreFish(10.0, listOf(3,3,3))
}

fun canAddFish(tankSize: Double,
                 currentFish: List<Int>,
                 fishSize: Int,
                 hasDecorations: Boolean = true): Boolean {
    return true
}

fun fitMoreFish() {
    var tankSize = 20.0
    var fishSize = listOf(x, y, z)

}